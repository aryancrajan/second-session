// function diffNumbers(a,b){
//     return Math.abs(a-b);}
var a=5;
var b=8;

function diff(a,b)
    {
    return a-b
    }

function abs(c)
    {
    if(c>0)
        return c
    else
        return -1*c
    }
c=diff(a,b)
console.log(abs(c))
